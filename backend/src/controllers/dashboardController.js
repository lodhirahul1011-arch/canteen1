import Order from '../models/Order.js';import Food from '../models/Food.js';import User from '../models/User.js';
export const dashboard=async(req,res)=>{const [orders,foods,members,staff,revenue]=await Promise.all([Order.countDocuments(),Food.countDocuments(),User.countDocuments({role:'member'}),User.countDocuments({role:'staff'}),Order.aggregate([{$match:{status:{$ne:'CANCELLED'},paymentStatus:{$ne:'FAILED'}}},{$group:{_id:null,total:{$sum:'$total'}}}])]);res.json({orders,foods,members,staff,revenue:revenue[0]?.total||0,lowStock:await Food.countDocuments({$expr:{$lte:['$stock','$lowStockLimit']}})})};